# v2.1test 开工前必读

本项目的目标是用统一设计系统维护整套交互演示，同时保留每页对技术内容和专属可视化的所有权。修改代码前先读本文件和 `README.md`。

## 必须遵守

- 项目内只使用相对路径；不依赖 CDN、远程运行时或本机绝对路径。
- 可见 HTML 文本和 SVG 文本不得小于 `11px`，不得绕过 `--text-*` 语义字号。
- 字体只从 `--font-sans`、`--font-mono`、`--font-serif` 选择。
- 字号、行高、字重、颜色、间距、边框、圆角、控件尺寸、符号尺寸、动画和图表线型优先使用 `themes/default/tokens.css`。
- JS 动画时长和图表常量使用 `shared/design/tokens.js`，不得在页面内复制同一套数字。
- 通用分段控件、按钮、输入、面板、表格、标签、说明、公式和图例优先复用 `shared/styles/base.css`。
- 图标按钮优先使用熟悉的符号或项目已启用的图标库；不手绘重复图标。陌生符号必须有 `title` 或可访问名称。
- 字母、数字、公式、代码和轴标签使用等宽字体；正文和操作文本使用无衬线字体；长篇引述才使用衬线字体。
- 字间距统一为 `0`，不得用负字距压缩内容。
- 放不下时先调整布局、网格和间距，不得降低到最小字号以下。

## 全局层负责

- 字体角色和最小字号
- 语义颜色、背景、描边、圆角和阴影
- 间距、控件尺寸、焦点态和动画节奏
- 常见面板、表格、按钮、输入、分段控件和图例
- SVG 标签字号、常见线宽和虚线节奏
- 全局导航、播放、编辑和键盘交互

## 页面层负责

- 技术内容、公式、数据和来源
- SVG 坐标、viewBox、曲线和领域图形
- 页面专属布局比例与局部响应式规则
- step、tab、slider 等领域状态和交互逻辑
- 只在本页出现、无法合理复用的视觉编码

页面不得反向修改 `shared` 行为。只有至少两个页面共享且语义一致的模式，才提升到全局层。

## 修改顺序

1. 先确认需求属于全局层还是页面层。
2. 查找现有 token 或共享样式，确认没有等价定义。
3. 修改最小范围代码；不要为未来需求新增抽象。
4. 检查 `1562×1188` 下全部页面及复杂 tab/step 状态。
5. 运行 `npm run check`，并检查浏览器控制台。

## 完成标准

- 8 个页面可见文字最小计算字号不低于 `11px`。
- 页面与内部主视图区无水平或垂直裁切。
- 键盘、tab、step、slider 和按钮行为正常，焦点清晰可见。
- 浏览器控制台无项目代码错误。
- `npm run check` 通过。
- 原始 `v2test` 不被修改。
